// how to install npm modules

//console.log('testing');

// installing nodemon 
// npm i nodemon -g
// nodemon helps to run the code in node while having nodemon installed we don't have to write node filename in the terminal every single time. every time we press ctrl + s (save) nodemon will do node filename for us****

// node dependecies have huge number of modules
// so we don't want these files in github 
// so add .gitignore file
//----------------------------------------------------------------//

const { format } = require('date-fns');

console.log(format(new Date(), 'yyyyMMdd\tHH:mm:ss'));









